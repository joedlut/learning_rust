fn main() {
    //shadow can change the type of the variable
    let spaces = "   ";
    println!("the values of spaces is {}",spaces);
    let spaces = spaces.len();
    println!("the values of spaces is {}",spaces);
}
